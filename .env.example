# Customer Engagement Prediction Platform - Environment Variables
# Copy this file to .env and fill in your values
# For production, use AWS Secrets Manager or SSM Parameter Store instead of .env files

# =============================================================================
# AWS Configuration
# =============================================================================

# AWS Region (e.g., us-east-1, us-west-2)
AWS_REGION=us-east-1
AWS_DEFAULT_REGION=us-east-1

# AWS Credentials (for LocalStack development only - use IAM roles in production)
# These test values work with LocalStack
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# LocalStack API Key (optional, for LocalStack Pro features)
# LOCALSTACK_API_KEY=your-localstack-api-key

# =============================================================================
# Application Environment
# =============================================================================

# Environment name (dev, staging, prod)
ENV=dev

# =============================================================================
# S3 Buckets
# =============================================================================

# Raw data bucket (input data)
# Example: poc-engagement-raw-data-dev
RAW_BUCKET=your-raw-data-bucket-name

# Processed data bucket (train/test splits)
# Example: poc-engagement-processed-data-dev
PROCESSED_BUCKET=your-processed-data-bucket-name

# Features bucket (engineered features)
# Example: poc-engagement-features-dev
FEATURES_BUCKET=your-features-bucket-name

# Models bucket (trained ML models)
# Example: poc-engagement-models-dev
MODELS_BUCKET=your-models-bucket-name

# Results bucket (prediction outputs)
# Example: poc-engagement-results-dev
RESULTS_BUCKET=your-results-bucket-name

# Athena query results bucket
# Example: poc-engagement-athena-results-dev
ATHENA_RESULTS_BUCKET=your-athena-results-bucket-name

# Knowledge base bucket (for Bedrock Knowledge Base)
# Example: poc-engagement-knowledge-base-dev
KNOWLEDGE_BASE_BUCKET=your-knowledge-base-bucket-name

# =============================================================================
# Glue Data Catalog
# =============================================================================

# Glue database for raw data
# Example: engagement_raw
GLUE_DATABASE_RAW=your-glue-database-raw

# Glue database for processed data
# Example: engagement_processed
GLUE_DATABASE_PROCESSED=your-glue-database-processed

# Glue database for ML features
# Example: engagement_ml
GLUE_DATABASE_ML=your-glue-database-ml

# =============================================================================
# Athena Configuration
# =============================================================================

# Athena workgroup name
# Example: engagement-workgroup
ATHENA_WORKGROUP=your-athena-workgroup-name

# =============================================================================
# DynamoDB Tables
# =============================================================================

# DynamoDB table for prediction caching
# Example: engagement-predictions-cache
DYNAMODB_TABLE=your-dynamodb-table-name

# =============================================================================
# ML Model Configuration
# =============================================================================

# Model version tag (e.g., v1.0, v1.1)
MODEL_VERSION=v1.0

# Prediction cache TTL in seconds (default: 1 hour)
CACHE_TTL_SECONDS=3600

# QA sample size for model validation
QA_SAMPLE_SIZE=400

# =============================================================================
# Production Secrets (use AWS Secrets Manager instead)
# =============================================================================

# For production, store these in AWS Secrets Manager:
# - Database connection strings
# - API keys (if any)
# - Third-party service credentials
#
# Example retrieval in Lambda/Fargate:
# import boto3
# secrets_client = boto3.client('secretsmanager')
# secret = secrets_client.get_secret_value(SecretId='engagement-platform/secrets')
# secrets = json.loads(secret['SecretString'])
#
# See docs/security/secrets_management.md for details

# =============================================================================
# Notes
# =============================================================================

# 1. Never commit .env files to version control
# 2. Use AWS Secrets Manager or SSM Parameter Store for production secrets
# 3. Use IAM roles for AWS service authentication (not access keys)
# 4. For LocalStack development, test credentials are acceptable
# 5. All bucket names and resource names should follow your naming convention
